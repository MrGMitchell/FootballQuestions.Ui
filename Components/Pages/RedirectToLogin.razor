@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Force a hard redirect to the Entra ID sign-in path
        Navigation.NavigateTo("MicrosoftIdentity/Account/SignIn", forceLoad: true);
    }
}